interface:
  display_name: "vLLM-Omni Deck"
  short_description: "Build and update evidence-backed vLLM-Omni decks"
  default_prompt: "Use $vllm-omni-deck to update the community release deck from the latest stable tag, preserve approved slides, rank evidence-backed highlights, and validate the rendered layout."
