interface:
  display_name: "Deck Compiler"
  short_description: "Validate deck specs, dependencies, and revisions"
  default_prompt: "Use $deck-compiler to validate this approved deck spec, report narrative and dependency issues, and produce a deterministic manifest."
